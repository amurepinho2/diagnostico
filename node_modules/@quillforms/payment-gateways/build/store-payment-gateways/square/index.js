"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");
var _config = _interopRequireDefault(require("@quillforms/config"));
var _hooks = require("@wordpress/hooks");
var _api = require("../../api");
var _customerRender = _interopRequireDefault(require("../customer-render"));
/**
 * QuillForms Dependencies.
 */

/**
 * WordPress Dependencies.
 */

/**
 * Internal Dependencies.
 */

(0, _hooks.addAction)('QuillForms.Admin.PluginsLoaded', 'QuillForms/PaymentGateways/RegisterStoreModules', register);
(0, _hooks.addAction)('QuillForms.RendererCore.Loaded', 'QuillForms/PaymentGateways/RegisterStoreModules', register);
function register() {
  const assetsDir = _config.default.getPluginDirUrl() + 'assets/addons/square';
  (0, _api.registerPaymentGatewayModule)('square', {
    name: 'Square',
    description: 'Accept payments through square gateway.',
    icon: {
      mini: `${assetsDir}/icon.png`,
      full: `${assetsDir}/icon-full.svg`
    },
    active: false,
    methods: {
      card: {
        configured: false,
        isRecurringSupported: true,
        admin: {
          label: {
            icon: `${assetsDir}/icon-full.svg`,
            text: 'Card'
          }
        },
        customer: {
          label: {
            text: 'Square Card'
          },
          render: _customerRender.default
        }
      },
      applePay: {
        configured: false,
        isRecurringSupported: false,
        admin: {
          label: {
            icon: `${assetsDir}/icon-full.svg`,
            text: 'Apple Pay'
          }
        },
        customer: {
          label: {
            text: 'Square Apple Pay'
          },
          render: _customerRender.default
        }
      },
      googlePay: {
        configured: false,
        isRecurringSupported: false,
        admin: {
          label: {
            icon: `${assetsDir}/icon-full.svg`,
            text: 'Google Pay'
          }
        },
        customer: {
          label: {
            text: 'Square Google Pay'
          },
          render: _customerRender.default
        }
      }
    }
  });
}
//# sourceMappingURL=index.js.map
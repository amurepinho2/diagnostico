"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _react = require("react");
var _mappingRow = _interopRequireDefault(require("../mapping-row"));
/**
 * WordPress Dependencies
 */

/**
 * Internal Dependencies
 */

const MappingList = ({
  sections,
  options,
  initial,
  onChange
}) => {
  // initial mapping rows.
  const initialMappingRows = (0, _react.useMemo)(() => {
    const result = [];
    // first add the initial fields.
    for (const [key, value] of Object.entries(initial !== null && initial !== void 0 ? initial : {})) {
      result.push({
        key,
        value
      });
    }
    // then add the required options.
    for (const option of options !== null && options !== void 0 ? options : []) {
      if (option.required && !result.find(item => item.key === option.value)) {
        result.push({
          key: option.value,
          value: {}
        });
      }
    }
    // add an empty option if there is no options added.
    if (result.length === 0) {
      result.push({
        key: '',
        value: {}
      });
    }
    return result;
  }, []);

  // mapping rows state.
  const [mappingRows, setMappingRows] = (0, _react.useState)(initialMappingRows);
  const selectedKeys = mappingRows.map(row => row.key);

  // call onChange
  (0, _react.useEffect)(() => {
    const fields = {};
    for (const {
      key,
      value
    } of mappingRows) {
      if (key && value.type && value.value) {
        fields[key] = value;
      }
    }
    onChange(fields);
  }, [mappingRows]);
  return (0, _react.createElement)("div", null, mappingRows.map((fieldRow, index) => {
    const option = options?.find(option => option.value === fieldRow.key);
    return (0, _react.createElement)(_mappingRow.default, {
      key: index,
      keyProps: {
        sections: sections,
        options: options?.filter(option => option.value === fieldRow.key || !selectedKeys.includes(option.value)),
        value: fieldRow.key,
        onChange: key => {
          setMappingRows(state => {
            const newState = [...state];
            newState[index] = {
              key,
              value: {}
            };
            return newState;
          });
        },
        disabled: option?.required
      },
      valueProps: {
        value: fieldRow.value,
        onChange: value => {
          setMappingRows(state => {
            const newState = [...state];
            newState[index].value = value;
            return newState;
          });
        },
        customize: option?.combobox?.customize
      },
      onAddClick: () => {
        setMappingRows(state => {
          const newState = [...state];
          newState.splice(index + 1, 0, {
            key: '',
            value: {}
          });
          return newState;
        });
      },
      onRemoveClick: option?.required || mappingRows.length === 1 ? undefined : () => {
        setMappingRows(state => {
          const newState = [...state];
          newState.splice(index, 1);
          return newState;
        });
      }
    });
  }));
};
var _default = exports.default = MappingList;
//# sourceMappingURL=index.js.map
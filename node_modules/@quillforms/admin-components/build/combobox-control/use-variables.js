"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _data = require("@wordpress/data");
var _lodash = require("lodash");
/**
 * QuillForms Dependencies
 */

/**
 * WordPress Dependencies
 */

/**
 * External Dependencies
 */

const useVariables = ({
  section
}) => {
  const {
    variables
  } = (0, _data.useSelect)(select => {
    var _ref;
    return {
      variables: (_ref = select('quillForms/logic-editor'
      // @ts-ignore.
      )?.getLogic()?.variables) !== null && _ref !== void 0 ? _ref : {}
    };
  });
  const fields = [];
  if ((0, _lodash.size)(variables) > 0) {
    for (const [id, variable] of Object.entries(variables)) {
      fields.push({
        type: 'variable',
        value: id,
        label: variable?.label,
        iconBox: {},
        section,
        isMergeTag: true
      });
    }
  }
  return fields;
};
var _default = exports.default = useVariables;
//# sourceMappingURL=use-variables.js.map
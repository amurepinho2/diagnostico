import { createElement } from "react";
/**
 * WordPress Dependencies
 */
import { createSlotFill } from '@wordpress/components';
import React from 'react';
const {
  Fill,
  Slot
} = createSlotFill('formAdminBar');
const FormAdminNavSlot = props => {
  return createElement(Slot, {
    ...props
  });
};
const FormAdminNavFill = ({
  children
}) => {
  return createElement(Fill, null, children);
};
const FormAdminNav = FormAdminNavFill;
FormAdminNav.Slot = FormAdminNavSlot;
export default FormAdminNav;
//# sourceMappingURL=admin-nav.js.map
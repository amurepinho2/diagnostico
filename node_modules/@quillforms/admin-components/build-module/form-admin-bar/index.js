import { createElement, Fragment } from "react";
/**
 * QuillForms Dependencies
 */
import { NavLink, withRouter } from '@quillforms/navigation';

/**
 * WordPress Dependencies
 */

import { Icon } from '@wordpress/components';
import { arrowLeft } from '@wordpress/icons';

/**
 * Internal Dependencies
 */
import Logo from '../logo';
import FormAdminNav from './admin-nav';
const FormAdminBar = ({
  formId
}) => {
  return createElement("div", {
    className: "admin-components-form-admin-bar"
  }, createElement("div", {
    className: "admin-components-form-admin-bar__logo"
  }, createElement(Logo, null)), createElement(NavLink, {
    className: "admin-components-form-admin-bar__home-link",
    to: `/admin.php?page=quillforms`
  }, createElement(Icon, {
    icon: arrowLeft
  }), " Back to home"), createElement(FormAdminNav.Slot, null, fills => createElement(Fragment, null, createElement(NavLink, {
    isActive: (_match, location) => {
      if (location.pathname === `/forms/${formId}/builder`) {
        return true;
      }
    },
    activeClassName: "selected",
    to: `/admin.php?page=quillforms&path=/forms/${formId}/builder`
  }, "Design"), createElement(NavLink, {
    isActive: (_match, location) => {
      if (location.pathname.startsWith(`/forms/${formId}/share`)) {
        return true;
      }
    },
    activeClassName: "selected",
    to: `/admin.php?page=quillforms&path=/forms/${formId}/share`
  }, "Share"), createElement(NavLink, {
    isActive: (_match, location) => {
      if (location.pathname.startsWith(`/forms/${formId}/results`)) {
        return true;
      }
    },
    activeClassName: "selected",
    to: `/admin.php?page=quillforms&path=/forms/${formId}/results`
  }, "Results"), createElement(NavLink, {
    isActive: (_match, location) => {
      if (location.pathname.startsWith(`/forms/${formId}/integrations`)) {
        return true;
      }
    },
    activeClassName: "selected",
    to: `/admin.php?page=quillforms&path=/forms/${formId}/integrations`
  }, "Integrations"), createElement(NavLink, {
    isActive: (_match, location) => {
      if (location.pathname.startsWith(`/forms/${formId}/payments`)) {
        return true;
      }
    },
    activeClassName: "selected",
    to: `/admin.php?page=quillforms&path=/forms/${formId}/payments`
  }, "Payments", createElement("div", {
    className: "admin-components-control-label__new-feature"
  }, "NEW")), fills)));
};
export default withRouter(FormAdminBar);
//# sourceMappingURL=index.js.map
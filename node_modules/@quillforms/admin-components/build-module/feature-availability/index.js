import { createElement } from "react";
/**
 * QuillForms Dependencies
 */
import ConfigApi from '@quillforms/config';

/**
 * WordPress Dependencies
 */
import { Icon } from '@wordpress/components';

/**
 * External Dependencies
 */
import { css } from 'emotion';

/**
 * Internal Dependencies
 */
import lockIcon from './lock-icon';
const FeatureAvailability = ({
  featureName,
  planKey,
  showLockIcon
}) => {
  const license = ConfigApi.getLicense();
  const featurePlanLabel = ConfigApi.getPlans()[planKey].label;
  const isPlanAccessible = ConfigApi.isPlanAccessible(planKey);
  let content = createElement("div", null);
  if (license?.status === 'valid' && isPlanAccessible) {
    content = createElement("div", null, featureName, " is already available on your plan.");
  } else {
    content = createElement("div", null, showLockIcon && createElement(Icon, {
      className: "feature-availability-lock-icon",
      icon: lockIcon,
      size: 120
    }), createElement("p", {
      className: css`
						font-size: 15px;
					`
    }, "We're sorry, ", featureName, " is not available", createElement("br", null), "on your plan. Please upgrade to the ", featurePlanLabel, ' ', "plan to unlock", createElement("br", null), "all of ", featurePlanLabel, " features."), createElement("a", {
      href: "https://quillforms.com",
      target: "_blank",
      className: "feature-availability-upgrade-button"
    }, "Upgrade to ", featurePlanLabel, "!"));
  }
  return createElement("div", {
    className: "feature-availability"
  }, content);
};
export default FeatureAvailability;
//# sourceMappingURL=index.js.map
import { createElement } from "react";
/**
 * WordPress Dependencies
 */
import { plus, trash } from '@wordpress/icons';
import { Icon } from '@wordpress/components';

/**
 * Internal Dependencies
 */
import { default as MappingKeyControl } from './key-control';
import { default as ComboboxControl } from '../combobox-control';
const MappingRow = ({
  keyProps,
  valueProps,
  onAddClick,
  onRemoveClick
}) => {
  return createElement("div", {
    className: "mapping-row"
  }, createElement(MappingKeyControl, {
    ...keyProps
  }), createElement("div", {
    className: "mapping-row-separator"
  }), createElement(ComboboxControl, {
    ...valueProps
  }), createElement("div", {
    className: "mapping-row-buttons"
  }, !!onRemoveClick && createElement("div", {
    className: "mapping-row-buttons-remove"
  }, createElement(Icon, {
    icon: trash,
    onClick: onRemoveClick
  })), !!onAddClick && createElement("div", {
    className: "mapping-row-buttons-add"
  }, createElement(Icon, {
    icon: plus,
    onClick: onAddClick
  }))));
};
export default MappingRow;
//# sourceMappingURL=index.js.map
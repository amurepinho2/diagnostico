import { createElement } from "react";
/**
 * Internal Dependencies
 */
import { MappingKeyControlContextProvider } from './context';
import Text from './text';
import Select from './select';
const MappingKeyControl = ({
  sections = [],
  options,
  value,
  onChange,
  disabled
}) => {
  return createElement("div", {
    className: "mapping-key-control"
  }, createElement(MappingKeyControlContextProvider, {
    value: {
      value,
      onChange,
      disabled
    }
  }, options ? createElement(Select, {
    sections: sections,
    options: options
  }) : createElement(Text, null)));
};
export default MappingKeyControl;
//# sourceMappingURL=index.js.map
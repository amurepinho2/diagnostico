import { createElement } from "react";
/**
 * QuillForms Dependencies
 */

/**
 * WordPress Dependencies
 */
// @ts-expect-error
import { Icon } from '@wordpress/components';
import { plus } from '@wordpress/icons';

/**
 * External Dependencies
 */

import classnames from 'classnames';

/**
 * Internal Dependencies
 */
import BlockIconWrapper from './wrapper';
const BlockIconBox = ({
  icon,
  order,
  color
}) => {
  icon = icon ? icon : plus;
  color = color ? color : '#bb426f';
  const renderedIcon = createElement(Icon, {
    icon: icon?.src ? icon.src
    // @ts-expect-error
    : icon
  });
  return createElement(BlockIconWrapper, {
    color: color
  }, renderedIcon, order && createElement("span", {
    className: classnames('admin-components-block-icon-box__order', {
      'black-color': color === '#fff'
    })
  }, order));
};
export default BlockIconBox;
//# sourceMappingURL=index.js.map
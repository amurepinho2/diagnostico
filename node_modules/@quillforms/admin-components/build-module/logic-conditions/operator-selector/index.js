import { createElement } from "react";
/**
 * QuillForms Dependencies
 */

import SelectControl from '../../select-control';
const options = [{
  key: 'is',
  name: 'is equal to'
}, {
  key: 'is_not',
  name: 'is not equal to'
}, {
  key: 'lower_than',
  name: 'lower than'
}, {
  key: 'greater_than',
  name: 'greater than'
}, {
  key: 'starts_with',
  name: 'starts with'
}, {
  key: 'ends_with',
  name: 'ends with'
}, {
  key: 'contains',
  name: 'contains'
}, {
  key: 'not_contains',
  name: "doesn't contain"
}];
const OperatorSelector = ({
  value,
  onChange,
  operators
}) => {
  const $options = options.filter(option =>
  // @ts-ignore
  operators.includes(option.key));
  if (!value) {
    setTimeout(() => onChange(operators[0]));
    return null;
  }
  return createElement("div", null, createElement(SelectControl, {
    value: options.find(option => option.key === value),
    onChange: selectedChoice => {
      if (selectedChoice && selectedChoice.selectedItem) {
        onChange(selectedChoice.selectedItem.key);
      }
    },
    options: $options
  }));
};
export default OperatorSelector;
//# sourceMappingURL=index.js.map
"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");
var _blocks = require("@quillforms/blocks");
var _settings = _interopRequireDefault(require("./settings"));
/**
 * QuillForms Dependencies
 */
/**
 * Internal Dependencies
 */
const {
  name: name
} = {
  name: "date",
  supports: {
    editable: true,
    required: true,
    attachment: true,
    description: true,
    logic: false,
    theme: true
  },
  attributes: {
    format: {
      type: "string",
      "enum": ["MMDDYYYY", "DDMMYYYY", "YYYYMMDD"],
      "default": "MMDDYYYY"
    },
    separator: {
      type: "string",
      "enum": ["/", "-", "."],
      "default": "/"
    }
  }
};
(0, _blocks.setBlockAdminSettings)(name, _settings.default);
//# sourceMappingURL=index.js.map
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.normalizeIconObject = normalizeIconObject;
var _isValidIcon = require("./is-valid-icon");
/**
 * QuillForms Dependencies
 */

/**
 * Internal Dependencies
 */

/**
 * Function that receives an icon during the registration
 * and returns a new icon object that is normalized so we can rely on just on possible icon structure
 * in the codebase.
 *
 * @param {IconRenderer} icon Render behavior of icon;
 *                            one of a Dashicon slug, an element, or a
 *                            component.
 *
 * @return {IconDescriptor} Object describing the icon.
 */
function normalizeIconObject(icon) {
  if (!icon) icon = 'plus';
  if ((0, _isValidIcon.isValidIcon)(icon)) {
    return {
      src: icon
    };
  }
  return icon;
}
//# sourceMappingURL=normalize-icon-object.js.map
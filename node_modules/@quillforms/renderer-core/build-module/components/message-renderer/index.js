import { createElement } from "react";
/**
 * Internal Dependencies
 */
import HtmlParser from '../html-parser';
const MessageRenderer = ({
  value
}) => {
  return createElement(HtmlParser, {
    value: value
  });
};
export default MessageRenderer;
//# sourceMappingURL=index.js.map